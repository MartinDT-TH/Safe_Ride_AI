import 'package:dio/dio.dart'; void main() { final dio = Dio(BaseOptions(baseUrl: 'http://127.0.0.1:5026/api/')); try { var req = dio.options.compose(dio.options, '/api/maps/reverse', data: null); print('Resolved /api/maps/reverse: ' + req.uri.toString()); var req2 = dio.options.compose(dio.options, '/maps/reverse', data: null); print('Resolved /maps/reverse: ' + req2.uri.toString()); var req3 = dio.options.compose(dio.options, '/auth/me', data: null); print('Resolved /auth/me: ' + req3.uri.toString()); } catch (e) { print(e); } }
